
  # Update Hero Background Image

  This is a code bundle for Update Hero Background Image. The original project is available at https://www.figma.com/design/0eyaklIITT6ZD8EcSZ6kQV/Update-Hero-Background-Image.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  